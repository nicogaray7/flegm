# flegm-platform

## Deploying to Vercel

The Next.js app lives in the **`web/`** directory. In Vercel, set **Root Directory** to **`web`** (Settings → General → Root Directory). Otherwise the build will fail with "Couldn't find any `pages` or `app` directory".
