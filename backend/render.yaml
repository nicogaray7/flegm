services:
  - type: web
    name: flegm
    env: node
    region: Oregon
    buildCommand: cd backend && yarn install && yarn build
    startCommand: cd backend && yarn start
    healthCheckPath: /api/health
    healthCheckTimeout: 5
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: CLOUDINARY_CLOUD_NAME
        value: dfupyozxe
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      - key: FRONTEND_URL
        value: https://flegm.vercel.app
    scaling:
      minInstances: 1
      maxInstances: 1
      targetMemoryPercent: 80
    buildFilter:
      paths:
        - backend/**/*.js
        - backend/**/*.ts
        - backend/package*.json 